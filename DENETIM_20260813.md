# Sufle — Başlangıç Denetimi (2026-08-13)

Kod değiştirilmeden, yalnız gözlemle yapıldı. Her bulgunun altında koddan/komuttan kanıt var.

---

## 0. Doğrulanan proje ve depo gerçeği

| Şey | Gerçek durum |
|---|---|
| Sürümlenen depo | `~/Desktop/.sufle-deploy` → `github.com/Erdalk05/sufle`, branch `main`, origin ile **eşit** (unpushed commit yok) |
| Telefon "master" klasörü | `~/Desktop/iPhone Teleprompter` — **Sufle deposunda değil**. `git rev-parse --show-toplevel` burada `/Users/erdalkiziroglu` (Sprinta'nın ev deposu) döndürüyor |
| Canlı | `https://erdalk05.github.io/sufle/` → `VER='9.0'`, `sw.js` → `sufle-v72` |
| Son commit | `a6d1ff2 v9.0 — goruntu donmasi gorulur oldu + celisen ses alarmi duzeltildi` |
| Çalışma ağacı | `M .son-yayin`, `M mac/Teleprompter Pro.html` (commit'siz) |
| Teknoloji | Tek dosya HTML + gömülü JS, **sıfır bağımlılık**. Paket yöneticisi yok, veritabanı yok (localStorage + IndexedDB), sunucu yok. Yayın: GitHub Pages |
| Test | `node tests/kos.js` — 20 dosya, **732 test, 0 hata** (baseline yeşil) |
| Kapı | `./kapi.sh` — 5 adım. **Şu an KIRMIZI** (aşağıda) |
| Roller | Yok. Tek kullanıcı, tek cihaz, hesap/kimlik yok. Çok kiracılılık, RLS, ödeme, KVKK veri işleme **kapsam dışı** — hiçbir veri cihazdan çıkmıyor |

**Görev planının durumu:** `GOREV_PLANI.md` içindeki 12 modül × 10 görev = 120 maddenin **hepsi ✅**. O plan kapandı; aşağıdaki liste yeni denetimden çıktı.

---

## P0 — Şimdi

### P0-1 · Yayınlanabilir kod sürümlenmeyen bir klasörde duruyor
**Problem.** Telefonun asıl dosyası `~/Desktop/iPhone Teleprompter/index.html` hiçbir git deposunda değil. Depodaki `.sufle-deploy/index.html` ondan farklı.

**Kanıt.**
```
md5  iPhone Teleprompter/index.html   31df5b5f733efc30af012a9c327e9118   (298772 B, 12 Ağu 18:25)
md5  .sufle-deploy/index.html         510e01acecb9f2c297063d54f8c7d729   (298098 B, 12 Ağu 17:56)
git rev-parse --show-toplevel  (iPhone Teleprompter içinde)  →  /Users/erdalkiziroglu
```
**Kullanıcıya etkisi.** Dolaylı ama en pahalısı: telefonun tek düzeltmesi yedeksiz. Klasör silinirse geri dönüşü yok. Ayrıca `kapi.sh`'in kendi yorumunda yazılı "sürüm sapması" hata sınıfının kök nedeni tam olarak bu ikilik.

**Etkilenen.** `index.html`, `denetim.py`, `GOREV_PLANI.md`, `PUAN_KARSILASTIRMA.md` — dördü de iki yerde.
**Bağımlılık.** Yok; ilk iş bu olmalı.
**Çözüm.** Master'daki değişikliği depoya taşı, diff'in beklenen tek değişiklik olduğunu kanıtla, master'ı depo kopyasıyla eşitle.
**Kabul.** `md5` eşit · 732 test yeşil · `denetim.py` temiz.
**Risk.** Düşük — geri alınabilir, dosya kopyası.

---

### P0-2 · "Ses çok kısık" yanlış alarmı telefonda düzeltilmiş ama CANLIDA yok
**Problem.** Kaydın başında kullanıcı henüz konuşmamışken ortalama ses seviyesi düşük çıkıyor ve uygulama "🔈 Ses çok kısık — mikrofona yaklaş" diyor. Sonuç özetinde de aynı yanlış yazıyor.

**Kanıt.** `.sufle-deploy/index.html` (= canlı) eski mantıkta:
```js
else if(audStats.n>12 && audStats.sum/audStats.n<0.04) setAudBadge('low');
...
if(avg<0.05) return '🔈 Ses cok kisik — mikrofona yaklas';
```
`iPhone Teleprompter/index.html` (git dışı) düzeltilmiş: son 30 örneğin **tepesi** ölçülüyor, ses hiç yokken yargılamıyor.
Canlı doğrulama: `curl https://erdalk05.github.io/sufle/index.html | grep VER` → `VER='9.0'` (eski mantık).

**Yarım kalmışlığın şekli:** Mac tarafı `.sufle-deploy/mac/Teleprompter Pro.html` içinde düzeltilmiş ama **commit edilmemiş**; telefon tarafı yalnız depo dışı klasörde. Yani düzeltme üç parçaya bölünmüş, hiçbiri yayında değil.

**Kullanıcıya etkisi.** Doğrudan: kullanıcı sesi iyiyken mikrofona yaklaşıyor → bu sefer gerçekten kırpma alarmı alıyor. Yanlış yönlendirme.
**Bağımlılık.** P0-1.
**Kabul.** Telefon ve Mac aynı tepe-tabanlı mantıkta · yeni test bunu kilitliyor (P0-3) · kapı yeşil.
**Risk.** Düşük — eşik mantığı, davranış değişikliği görünür ve geri alınabilir.

---

### P0-3 · Bu düzeltmeyi hiçbir test tutmuyor
**Problem.** 732 testin hiçbiri ses rozeti eşiklerini sınamıyor. Eski mantık da yeni mantık da yeşil geçiyor — yani düzeltme yarın sessizce geri gelebilir.

**Kanıt.** `tests/11-ses-studyosu-ve-filtre.js` ve `tests/17-kritik-degerler.js` içinde `setAudBadge` / `audStats` / `maxTepe` geçmiyor; kapı 12 Ağustos'taki iki farklı mantıkla da 732/0 verdi.
**Kabul kriteri (kendi kendini kanıtlayan).** Kasıtla eski `sum/n<0.04` mantığına döndürüldüğünde test **kırılmalı**. Kırılmıyorsa test yalan söylüyor demektir.
**Risk.** Yok — yalnız test eklenir.

---

### P0-4 · Kapı kırmızı: sürüm artmamış
```
== 4/5 Sürüm tutarlılığı ==
  index.html VER = 9.0 · sw.js cache = sufle-v72
  ✗ VER artmamış — son yayınla aynı (9.0). sw.js cache'ini de artır.
⛔ KAPI KIRMIZI — yayınlama
```
Diğer 4 adım yeşil (statik denetim, JS sözdizimi, 732 test, platform tutarlılığı: telefon=Mac=9.0, Windows birebir).
Bu bir hata değil, **doğru çalışan kapı**: bekleyen değişiklikler var ama sürüm artmamış. P0-2 kapanınca 9.1 / `sufle-v73` yapılır.

---

## P1 — Sonra

### P1-1 · `denetim.py`'nin master kopyası 41 satır bayat
Depodaki sürümde olup master'dakinde **olmayanlar**:
- olay işleyicisi parantezsiz atama dedektörü (`$('#x').onclick = tanımsızFonksiyon` — bu depoda `copGeriAl` ile gerçekten yaşandı, ne denetim ne `node --check` yakalamıştı)
- Mac desteği (`is_mac`, `state.` öneki, Mac boş-catch tabanı 16)
- genişletilmiş dinamik id listesi (`sbErr, sbVer, readyBtn, takesBtn, scExport, scImport`)

**Etkisi.** Master klasöründen denetim koşturulursa kapı sessizce zayıf çalışır — ve kapı zayıfsa bulunmayan hata "yok" sanılır.

### P1-2 · Parite kapısının kör noktası: biyonik okuma
Telefonda var (8 eşleşme), Mac'te **hiç yok** (0). `tests/15-platform-paritesi.js` KORUMALAR listesinde satırı yok, bu yüzden kapı boşluğu görmüyor.
Doğru sıra: önce listeye satır ekle (kapı **kırmızıya dönmeli** — boşluğun gerçek olduğunun kanıtı), sonra ya Mac'e taşı ya da "uygulanmaz: sebep" yaz.

### P1-3 · Mac'te erişilebilirlik sıfır
`aria-*` sayısı: telefon **20**, Mac **0**. Mac'te `role=` de yok; 4 ikonlu/yazısız düğme yalnız `title=` ile etiketli.
Telefonda da kapsam kısmi: 33 ikonlu düğmeye 20 etiket.

---

## P2 — Cihaz gerekiyor (kodla kapatılamaz, Erdal'da)

`GOREV_PLANI.md`'de ✅ ama "⏳ CİHAZ GEREKLİ" damgalı 6 madde: 4.8 (iOS sesle takip + kayıt mikrofon çakışması), 6.10 (uzun çekimde ısınma/pil), 7.7 (iOS kompozit + ses), 8.4 (kayıt sırasında kilit), 8.7 (kırpma eşiği kalibrasyonu), 10.10 (Android Dosyalar'a kaydetme).

**En kritiği:** sonuç ekranındaki **paylaşım tanı satırı** ne yazıyor. "Fotoğraflara kaydetmiyor" teşhisi tamamen buna bağlı ve hâlâ cevapsız.

---

## Çürüttüklerim (yanlış çıkan hipotezlerim)

Dürüstlük için yazıyorum — grep sayımıyla "Mac'te eksik" sandığım, kaynakta doğrulayınca **var** çıkanlar:
Nefesle akış (VAD) · Ses Stüdyosu (`makeFxTrack`, `audioFx`) · mikrofon seçimi · bit hızı (`videoBitsPerSecond`) · paylaşım (`navigator.share`) · yüksek kontrast.
**Ders:** eşleşme sayısı kanıt değil; Mac dosyası farklı adlandırma kullanıyor (`state.` vs `st.`, TR adlar). Parite iddiası ancak kaynaktan okunarak kurulur.

---

## Bağımlılık sırası

```
T1 (tek kaynak + senkron)  ──►  T2 (rozet testi)  ──►  T3 (v9.1 + commit)  ──►  [ERDAL: yayın]
T4 (denetim.py tekil)      ──►  T5 (parite boşluğu)
T6 (Mac a11y)                   bağımsız
T7 (cihaz doğrulama)            ERDAL + gerçek telefon
```

## Zorunlu durma noktaları (kendiliğinden yapılmayacak)
- **Yayın / push** (GitHub Pages canlıya çıkar) — T3 sonunda onay istenecek
- Cihaz doğrulaması gereken 6 madde
- Sunucu/hesap gerektirenler (bulut senkron, ikinci cihaz kumandası, AI senaryo) — eskiden beri Erdal kararı

## İlk dilim önerisi
**T1** — telefondaki ses düzeltmesini depoya taşı, master'ı depo ile eşitle, diff'in beklenen tek değişiklik olduğunu kanıtla, 732 test + denetim.py ile doğrula, commit et.
Bu dilim yalnız dosya senkronu; davranış değişikliği getirmiyor ama kaybolma riskini bugün kapatıyor.
